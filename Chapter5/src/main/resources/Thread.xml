<?xml version="1.0" encoding="UTF-8" ?>
<suite name="multithread" parallel="methods" thread-count="3">
    <!--
    class级别：相同的classes tag下的用例在用同一个线程中执行
    test级别：不用的test标签下用例可以在不同线程下执行，相同的test tag下的用例只能在同一个线程中执行
    method级别：所有用例都可以在不同线程下执行
    thread-count：代表最大并发线程数
    xml文件的方式不能指定线程池，只有方法上才可以指定
    -->
    <test name="multithreadonxml">
        <classes>
            <class name="com.course.testng.multithread.MultiThreadOnXML"/>
        </classes>
    </test>
</suite>